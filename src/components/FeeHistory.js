import React from "react";
import Typography from '@mui/material/Typography';
import { Divider } from "@mui/material";

export const FeeHistory = () => {
    return (
    <div>
      <Typography variant="h6"  sx={{ pt: 2 }}>Not Available Fee History</Typography>
      <Divider/>
    </div>
 
  );
}